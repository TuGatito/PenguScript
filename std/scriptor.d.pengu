## PenguScript declaration binding generated by 'pengu bind'.
## Source header: stb_image_write.h
include "scriptor.h"

# -- Constants (#define) ------------------------------

const stbiw__ZHASH as i64 is 16384

# ------------------------------------------------------------------
# Types (structs, unions, enums, aliases)
# ------------------------------------------------------------------

alias stbi_write_func as ref to weave with context as ref to void, data as ref to void, size as int into void

# ------------------------------------------------------------------
# Functions (every name receives the insignia prefix)
# ------------------------------------------------------------------

insignia stbi_

declare write_png with filename as ref to char, w as int, h as int, comp as int, data as ref to void, stride_in_bytes as int into int

declare write_bmp with filename as ref to char, w as int, h as int, comp as int, data as ref to void into int

declare write_tga with filename as ref to char, w as int, h as int, comp as int, data as ref to void into int

declare write_hdr with filename as ref to char, w as int, h as int, comp as int, data as ref to f32 into int

declare write_jpg with filename as ref to char, x as int, y as int, comp as int, data as ref to void, quality as int into int

declare write_png_to_func with func as ref to stbi_write_func, context as ref to void, w as int, h as int, comp as int, data as ref to void, stride_in_bytes as int into int

declare write_bmp_to_func with func as ref to stbi_write_func, context as ref to void, w as int, h as int, comp as int, data as ref to void into int

declare write_tga_to_func with func as ref to stbi_write_func, context as ref to void, w as int, h as int, comp as int, data as ref to void into int

declare write_hdr_to_func with func as ref to stbi_write_func, context as ref to void, w as int, h as int, comp as int, data as ref to f32 into int

declare write_jpg_to_func with func as ref to stbi_write_func, context as ref to void, x as int, y as int, comp as int, data as ref to void, quality as int into int

declare flip_vertically_on_write with flip_boolean as int into void
