## PenguScript declaration binding generated by 'pengu bind'.
## Source header: tinyfiledialogs.h
include "tinyfiledialogs.h"

# ------------------------------------------------------------------
# Types (structs, unions, enums, aliases)
# ------------------------------------------------------------------

# ------------------------------------------------------------------
# Functions (every name receives the insignia prefix)
# ------------------------------------------------------------------

insignia tinyfd_

declare getGlobalChar with aCharVariableName as ref to char into ref to char

declare getGlobalInt with aIntVariableName as ref to char into int

declare setGlobalInt with aIntVariableName as ref to char, aValue as int into int

declare beep into void

declare notifyPopup with aTitle as ref to char, aMessage as ref to char, aIconType as ref to char into int

declare messageBox with aTitle as ref to char, aMessage as ref to char, aDialogType as ref to char, aIconType as ref to char, aDefaultButton as int into int

declare inputBox with aTitle as ref to char, aMessage as ref to char, aDefaultInput as ref to char into ref to char

declare saveFileDialog with aTitle as ref to char, aDefaultPathAndOrFile as ref to char, aNumOfFilterPatterns as int, aFilterPatterns as ref to ref to char, aSingleFilterDescription as ref to char into ref to char

declare openFileDialog with aTitle as ref to char, aDefaultPathAndOrFile as ref to char, aNumOfFilterPatterns as int, aFilterPatterns as ref to ref to char, aSingleFilterDescription as ref to char, aAllowMultipleSelects as int into ref to char

declare selectFolderDialog with aTitle as ref to char, aDefaultPath as ref to char into ref to char

declare colorChooser with aTitle as ref to char, aDefaultHexRGB as ref to char, aDefaultRGB as byte, aoResultRGB as byte into ref to char
